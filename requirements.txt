# NOTE: This file is kept for legacy compatibility.
# The source of truth for dependencies is pyproject.toml.
# For new installations, use: pip install -e .
# For development: pip install -e ".[dev]"
# For visualization tools: pip install -e ".[viz]"

# Core dependencies (required)
numpy>=1.24.0
pillow>=10.0.0
pydantic>=2.0.0
scipy>=1.10.0
PyWavelets>=1.4.0
scikit-learn>=1.3.0
joblib>=1.3.0
opencv-python>=4.8.0

# Optional visualization dependencies
# Install with: pip install -e ".[viz]"
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

